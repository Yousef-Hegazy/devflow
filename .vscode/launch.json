{
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run next",
            "runtimeExecutable": "next",
            "cwd": "${workspaceFolder}",
            "args": ["dev"]
        }
    ]
}